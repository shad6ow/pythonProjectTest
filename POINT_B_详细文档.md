# 基于自监督正常用电模式建模的异常阶段定位方法

## 一、具体内容

### 1.1 方法概述

针对智能电表异常用电检测中监督方法依赖标签、对未知异常泛化不足、且难以指出异常发生时段的问题，本文提出一种基于自监督正常用电模式建模的异常阶段定位方法，记为点B。其基本思路是不直接学习异常用户的特征，而是先在正常用户上学习正常用电流形，再以待测用户对该流形的偏离程度作为异常依据；在此基础上，通过弱监督注意力机制，在仅有用户级标签的条件下反推异常发生的连续月份区间。

点B由两部分组成：自监督正常流形分支负责度量每个月偏离正常的程度，弱监督注意力定位头负责将注意力聚焦到异常月份。整体流程为：构建月度多通道用电序列，在正常用户上训练正常流形并得到逐月偏离度，再由注意力定位头聚合为用户级异常分数并抽取异常区间，最后输出异常时段与形态。

### 1.2 问题定义

将每个用户表示为月度多通道序列 X_i ∈ R^{T×C}，其中 T 为月份数，C 为通道数，通道由点A的特征工程结果给出，包括月均值、月标准差、月最大值、月零值率、月缺失率、相对基准偏离等。本文采用多示例学习（Multiple Instance Learning, MIL）的形式化：每个用户视为一个由 T 个月份示例构成的"袋"，仅已知用户级弱标签 y_i ∈ {0,1}，月级标签未知。该设定与实际稽查场景一致——已知用户违章，但不知违章发生于哪几个月。

### 1.3 自监督正常流形分支

正常流形分支仅在正常（或低风险）用户上训练，包含两个自监督任务。掩码重构任务随机遮蔽部分月份或通道，由模型根据上下文恢复原序列，以学习正常序列的内部依赖，其重构损失记为 L_rec。未来窗口预测任务以前期月份预测后期月份，针对窃电常见的后期突降、持续下降形态增强敏感性，其预测损失记为 L_pred。两任务联合，得到逐月偏离度

d_{i,m} = RecError_{i,m} + PredError_{i,m},

并按用户内均值与标准差对 d_{i,m} 标准化，以消除用户用电规模差异对偏离量级的支配。

### 1.4 弱监督注意力-MIL 定位头

在逐月偏离度之上构造注意力定位头。设月级注意力权重为 a_{i,m}，用户级异常分数由偏离度的注意力加权聚合得到

s_i = Σ_m a_{i,m} · d_{i,m},

再经可学习的缩放与偏置后通过 sigmoid，与用户级弱标签做二元交叉熵监督。训练完成后，注意力分布 a_{i,·} 即异常发生时间的定位输出，并据此抽取连续异常区间 [ŝ_i, ê_i] 及其置信度。

为使定位输出为连续区间而非离散散点，并约束正常用户注意力平坦，总损失在交叉熵与自监督损失之外加入三项正则：

L = BCE(s_i, y_i) + λ_ss·L_ss + λ_tv·Σ_m |a_{i,m} − a_{i,m−1}| + λ_sp·‖a_{i,·}‖_1 + λ_neg·1[y_i=0]·‖a_{i,·}‖_2^2,

其中 L_ss = L_rec + L_pred 为自监督损失；全变差项 λ_tv 促使注意力形成连续阶段；稀疏项 λ_sp 抑制全月份均匀激活；负样本项 λ_neg 约束正常用户注意力平坦且偏离低。

### 1.5 关键实现要点

模型训练的稳定性与定位质量取决于若干实现细节。第一，各通道量级差异较大，未经标准化时自监督损失量级可达九万以上而无法收敛，逐通道 z-score 标准化（裁剪范围 ±5）后损失稳定于约 2.2。第二，自监督重构仅在正常用户上进行，避免异常用户污染正常流形而压低异常月份的偏离。第三，注意力的内容分支初始化为零，由偏离度引导项主导初期注意力，以避免冷启动阶段注意力与偏离相互失效。第四，正常与异常样本比例悬殊，交叉熵中对正样本施加约 10.7 倍权重，以维持模型对异常月份的关注。

主要超参数如下：训练 20 轮，学习率 0.001（AdamW），隐藏维度 64，注意力头数 4，编码器层数 2，前馈维度 128，dropout 0.1，掩码比例 0.25，批大小 512；正则权重 λ_ss=1.0、λ_tv=0.05、λ_sp=0.01、λ_neg=0.1。

## 二、与点A的关系

点A为"随机矩阵理论 + 双路径 Transformer + 决策树集成"的监督式融合检测框架。其随机矩阵理论从全局用户群体中提取结构性偏离，双路径 Transformer 从月度多通道序列中提取局部与全局时序表征，最终由梯度提升树集成完成判别，判别能力强，适合利用已有标签学习异常边界。

点A的局限在于：依赖异常标签，遇未知异常时泛化能力受限；输出为用户级异常概率，对异常起始时段缺乏解释；且未单独建立正常用电行为的基准。点B与点A因此构成能力分工，而非分数拼接。点A为监督判别，学习异常与正常的区分；点B为自监督建模，学习正常用户的演化规律。点A识别已知异常并输出用户级概率，回答"谁异常"；点B补充隐蔽与阶段性异常的定位，输出异常区间与形态，回答"何时、何种形态"。

二者关系可概括为：点A学习异常边界，点B学习正常行为流形；点A识别已知异常，点B补充未知与隐蔽异常；点A给出用户级异常概率，点B给出偏离度与异常月份区间。

需特别说明，点B不以提升点A的全局判别指标为目标。经验证，将点B特征直接并入点A集成并重训，全局 AUC 不升反降；面向点A漏检区的补救信号其判别上限约为 0.63；后期窗口门控方案对 F1 的改善可忽略。因此本文不将"特征融合提升指标"作为结论，点A的全局指标（AUC 0.875983，F1 0.516921）保持不变，点B的价值体现在点A不具备的定位与解释维度。

## 三、实验内容与结果

### 3.1 实验设置

实验在 SGCC 公开数据集上进行，采用全量用户。由于真实数据缺乏月级异常标注，本文在正常用户上注入已知月份的合成异常以构造月级真值。注入形态包括突降、持续低谷、零值与缓慢漂移四类，区间长度为 2 至 5 个月、位置随机，合成注入选取 500 名正常用户。定位质量以交并比（IoU）、点调整精确率/召回率/F1 度量，并与三种基线对比：原始偏离度阈值（事后处理基线）、均匀注意力，以及随机连续区间（公平基线，区间长度与真实异常相同、位置随机，用以隔离"已知异常位置"带来的优势）。模型训练 20 轮，并以 5 个随机种子（11、22、33、44、55）重复，逐用户样本量合计 2500，结果以均值、标准差与 95% 置信区间报告，配对显著性采用 Wilcoxon 符号秩检验。

### 3.2 定位质量

表1给出点B注意力定位与基线的平均 IoU 对比。点B相对公平随机区间基线的平均 IoU 提升约 41%。衡量注意力对异常用户聚焦程度的分离度指标（异常用户与正常用户最大注意力之差）在 5 个种子下稳定为正，均值 0.198，置信区间 [0.172, 0.223]，表明注意力确实集中于异常用户。点调整 F1 为 0.336（置信区间 [0.232, 0.440]）。

表1 定位质量对比（5 种子均值）

| 方法 | 平均 IoU | 95% 置信区间 |
|---|---|---|
| 点B注意力定位 | 0.159 | [0.111, 0.207] |
| 随机区间（公平基线） | 0.113 | [0.103, 0.123] |
| 偏离度阈值（事后处理） | 0.082 | [0.017, 0.147] |

### 3.3 异常形态分析

按注入形态拆分（表2），定位能力随形态差异显著。对零值与突降型异常定位精确，零值型 IoU 约为公平基线的 2.5 倍；对持续低谷型为边际水平；对缓慢漂移型则不可靠。本文就缓变形态尝试了一项受控改进（对注意力做时间平滑），但未达到预设的改进门槛（缓变型 IoU 0.109 仍低于均匀基线 0.117），故依据预先设定的停止准则终止调参，并如实界定方法的适用范围。

表2 各异常形态的定位 IoU（5 种子均值）

| 异常形态 | 平均 IoU | 评价 |
|---|---|---|
| 零值 | 0.302 | 强，约为随机基线 2.5 倍 |
| 突降 | 0.117 | 中等 |
| 持续低谷 | 0.108 | 边际 |
| 缓慢漂移 | 0.108 | 弱，不可靠 |

### 3.4 逐用户分布特性

点B的逐用户 IoU 呈双峰分布，即精确命中或完全落空（IoU 为 0）。因此在平均 IoU、点调整 F1 与注意力分离度等均值指标上，点B稳定优于包括公平基线在内的全部对照；但在逐用户中位数上，点B与公平随机区间基线持平（Wilcoxon 检验中位数差为 0，p < 1e-12）。这表明点B可为相当一部分用户（尤以突降、零值型为主）实现精确定位，而中位用户的定位尚不优于随机。该特性予以如实报告。

### 3.5 点A与点B的联合分析

将点A的用户级分数与标签同点B的定位输出按用户编号关联，共 42372 条记录，全部匹配成功，标签对齐校验通过。在点A判定为异常的 3615 名用户中，1807 名（49.99%）获得置信度高于队列中位数的连续异常区间，且这些区间多集中于后期月份。需说明的是，该 49.99% 由"置信度高于中位数"的定义切分得到，反映按置信度的优先级分层，而非定位准确率。表3给出三例高分用户的定位结果，其异常区间集中于第 25、32、33 等后期月份，与点A的高异常分相互印证。

表3 点A高分异常用户的点B定位示例

| 用户（前 8 位） | 点B预测异常区间（月） | 主导注意力月份 |
|---|---|---|
| FA12E44A | 32–33 | 33、32 |
| 80211A13 | 25 | 25 |
| 2511A5AF | 32–33 | 32、33 |

### 3.6 结果小结与局限

实验表明，点B在平均定位质量、点调整 F1 与注意力分离度上稳定优于包括公平随机基线在内的全部对照，对零值与突降型异常定位精确，并能与监督判别框架形成"谁异常—何时、何形态异常"的能力分工。方法的局限在于：对缓慢漂移型异常无法可靠定位；逐用户定位呈双峰特性，中位用户的定位不优于随机；约半数被判异常的用户可获得清晰的连续嫌疑区间，其余用户区间较为模糊。此外，月级真值来自合成注入，若能获得真实异常月份标注，可进一步将定位证据由合成数据提升至真实数据。
